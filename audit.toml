# cargo-audit configuration.
# https://docs.rs/cargo-audit
[advisories]
# `paste` is unmaintained (RUSTSEC-2024-0436) but has no known vulnerability and
# is only pulled transitively by frankenstein and tokenizers/ct2rs — nothing we
# can bump directly. Ignore the informational warning so it doesn't mask a real
# advisory; revisit when those upstreams drop the dependency.
ignore = ["RUSTSEC-2024-0436"]
